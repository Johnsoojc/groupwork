```

go to github.com, click on the "+" sign to create a new repo
$cd <folder name>

$git init
$git add .
$git commit -m "commit message"
$git remote add origin http://github.com/johnsoojc/xxx.git
$git push origin master

```
